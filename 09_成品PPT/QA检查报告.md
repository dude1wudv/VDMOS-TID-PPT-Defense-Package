# QA 检查报告

## 结论

**QA 通过。** 成品已完成生成后检查、字号与边界修复、重新渲染和第二轮复核。

## 交付完整性

| 检查项 | 结果 |
|---|---:|
| PPTX 页数 | 74 |
| PDF 是否生成 | 通过 |
| 逐页 PNG | 74/74 |
| 演讲者备注 | 74/74 |
| 页面接触表 | 已生成 |
| 素材缺失 | 0 |
| 空白页 | 0 |
| 越界对象 | 0 |
| PowerPoint 文本溢出 | 0 |
| 小于 8 pt 的文本 | 0 |

说明：复用原页的页码、章节眉题和 `SUPPORTED/QUALIFIED` 状态徽标保留 8 pt；它们属于导航与状态元数据，不是正文或图注。正文、结论与新增图注按投影阅读规格制作。

## 验证方式

1. 使用 PowerPoint 实际打开成品并导出 PDF。
2. 使用 PowerPoint 以 1600 × 900 渲染全部 74 页 PNG。
3. 生成 `contact_sheet.png` 进行全套页面密度、留白和一致性检查。
4. 使用 PowerPoint `TextFrame2.TextRange.BoundHeight` 对全部文本框执行溢出复核，最终 `overflow_count=0`。
5. 执行 PPTX 深度结构验证，结果 `ok=true`；仅有 `XSD_DIR_NOT_CONFIGURED` 环境提示，不影响文件合法性与打开。
6. 执行自动设计检查，结果 `ok=true`。自动工具对全幅背景、卡片底形与文字叠放产生启发式警告；经边界检查和实际渲染确认，不属于页面裁切或文本溢出。

## 第一轮发现与修复

- 复用原页存在 81 处 7.4–7.8 pt 的旧页脚、状态或边界文字。
- 封面完整课题名称在原文本框内高度不足。
- 部分复用页底部科学边界框扩容后接近页面边缘。

修复措施：

- 正式来源和科学边界文字提升至 10 pt。
- 原页章节眉题、页码和状态徽标统一为 8 pt，并扩大状态框高度。
- 封面标题保持 24 pt，扩大标题安全区并下移副标题。
- 底部科学边界框上移并固定安全高度。

## 第二轮结果

- 74 页重新渲染成功。
- 文本溢出：0。
- 页面越界：0。
- 素材缺失：0。
- 备注缺失：0。
- 科学禁用表述检查：`手动仿真` 0 次，`AI自主发现` 0 次；“盲测预测”仅出现在“不能称为盲测预测精度”的否定语境。

## 文件校验

- PPTX SHA-256：`5E283C26BAA2DBDD18CD59627C713606B644E66FD39211F89A0FE470BDB1425A`
- PDF SHA-256：`5241E6AB1A761261B5BD3BCE85D54E01BC49A8BDF90DB49FFCE8CA9913C4862B`

哈希对应最终渲染前的成品内容；若后续人工补充封面个人信息，需重新计算。